export const Movement = class {
    constructor(id, date, amount, description) {
      this.id = id;
      this.date = date;
      this.amount = amount;
      this.description = description;
    }
  }
